---
title: "単一HTMLだけで「受講生が問題を作れる」研修ツールを実装した話【新人向け】"
emoji: "🐰"
type: "tech"
topics: ["html", "javascript", "初心者", "frontend"]
published: false
---

## この記事について

ライブラリもサーバーも使わず、**HTMLファイル1つ** だけで動く研修ツールに、「受講生が問題・答え・ヒントを作れる」機能を追加しました。新人エンジニア・文系出身の方に向けて、**むずかしい道具を使わなくても、ちゃんと動くものは作れる** ということを、実際のコードで紹介します。

:::message
コード例は雰囲気をつかむための簡略版です。考え方の流れを追ってもらうのが目的です。
:::

---

## なぜ「単一HTML」なのか

研修ツールに求められるのは、まず **どこでも確実に動くこと** です。

- 会場のネットが不安定でも動いてほしい → **オフラインで動く**
- 受講生のPCに何もインストールさせたくない → **ファイルを開くだけ**
- CDN（外部の配信サーバー）が落ちても困らないようにしたい → **外部依存ゼロ**

これを全部満たす一番シンプルな答えが「HTML・CSS・JavaScript を1ファイルにまとめる」でした。豪華な道具より、**確実に動く道具** のほうが現場では強いのです。

---

## 機能1：タイマーの秒数を自由に決める

もともと「15秒」「30秒」のボタンしかなかったタイマーに、好きな秒数を入れられる入力欄を足しました。

### HTML（入力欄）

```html
<input type="number" id="secCustom" min="1" max="3600" placeholder="秒">
<button onclick="applyCustomSec()">設定</button>
```

`type="number"` にすると、数字以外が入りにくくなります。ブラウザが手伝ってくれるんですね。

### JavaScript（設定する処理）

```javascript
function applyCustomSec() {
  let v = parseInt(document.getElementById("secCustom").value, 10);
  if (!Number.isFinite(v)) return;        // 数字じゃなければ何もしない
  v = Math.max(1, Math.min(3600, v));     // 1〜3600 におさめる
  document.getElementById("secCustom").value = v;
  applySec(v);                            // タイマーに反映
}
```

ポイントは2つです。

1. **おかしな入力は無視する**：空っぽや文字が来たら、何もせず帰る（`return`）
2. **範囲をはみ出したら直す**：`Math.max(1, Math.min(3600, v))` で、0以下は1に、3600超は3600にそろえる

`Math.max` と `Math.min` を組み合わせると「下限と上限の間におさめる」が一行で書けます。これは **クランプ（clamp）** と呼ばれる定番の書き方なので、覚えておくと便利です。

:::message
ユーザーは予想外の入力をします。「マイナスを入れたら？」「空欄で押したら？」を最初から考えておくと、壊れにくいツールになります。
:::

---

## 機能2：受講生が問題を作る

ここが今回の目玉です。受講生が「問題・答え・ヒント」を入力して、問題集にためていけます。

### データのいれもの（配列）

問題は **配列** にためていきます。配列は「箱がならんだ棚」のイメージです。

```javascript
const qbank = [];  // 問題をためる棚
```

### 問題を1つ作る

入力された値を、ひとつのオブジェクト（ラベル付きのまとまり）にします。

```javascript
function buildQuestion(d) {
  const h = [];
  if (d.h1) h.push(d.h1);   // ヒント1が入力されていれば追加
  if (d.h2) h.push(d.h2);   // ヒント2も同様
  return { q: d.q, a: d.a, h: h, by: d.by || "", n: 0 };
}
```

`q`＝問題、`a`＝答え、`h`＝ヒントの配列、`by`＝作った人、`n`＝問題番号。
ラベルをつけてまとめておくと、あとで取り出すのが楽になります。

### 「追加」ボタンの処理

```javascript
function addQuestion() {
  const q = multiClean(qval("q-q"));   // 入力をきれいにする
  const a = multiClean(qval("q-a"));
  if (!q || !a) {                      // 問題か答えが空なら…
    showWarn("問題と答えは必須です");   // 警告を出して終わり
    return;
  }
  qbank.push(buildQuestion({ q, a, h1: ..., h2: ..., by: ... }));
  renumberQ();    // 番号をふり直す
  renderQBank();  // 画面に一覧を描き直す
  clearQForm();   // 入力欄を空にする
}
```

流れはとてもシンプルです。

> **入力チェック → 配列に追加 → 番号ふり直し → 画面更新 → 入力欄クリア**

このように「やることを順番に並べる」だけで、機能はできあがります。

### ヒントを1段ずつ見せる

ヒントは、いっぺんに全部見せると考える余地がなくなります。だから **1つずつ** 出します。

```javascript
let hintShown = 0;  // 何個ヒントを出したか

function revealHint() {
  if (hintShown >= curQ.h.length) return;   // もうない
  表示する(curQ.h[hintShown]);              // 次のヒントを出す
  hintShown++;                              // カウントを進める
}
```

「いま何個出したか」を数えておいて、次の1個を出す。シンプルですが、これで段階的なヒントが実現できます。

---

## 学びになったこと

- **道具より考え方**：フレームワークがなくても、配列と関数だけで機能は作れる
- **おかしな入力を先に考える**：空欄・文字・範囲外を最初から想定する
- **処理は「順番に並べる」だけ**：チェック→更新→クリア、と分けて書くと読みやすい
- **ユーザーの気持ちを想像する**：ヒントを1段ずつ、は「考える時間をうばわない」配慮

「すごいライブラリを使うこと」がエンジニアリングではありません。**目の前の人が困らないように、確実に動くものを作ること** が出発点だと、このツールを作って改めて思いました。

次は、あなたも「ファイル1つで動く小さな道具」を作ってみませんか？🐰
